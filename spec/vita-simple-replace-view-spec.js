'use babel';

import VitaSimpleReplaceView from '../lib/vita-simple-replace-view';

describe('VitaSimpleReplaceView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
